(ns groceries.core
  (:gen-class))

(defn solve [list]
  (println "make it work!"))


(defn solve-discount [list]
  (println "make it work!"))

(defn solve-special-offer [list]
  (println "make it work!"))
  
